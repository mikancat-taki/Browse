services:
  - type: web
    name: pppppproxy
    env: node
    plan: free
    buildCommand: "yarn install"
    startCommand: "node backend/server.js"
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
